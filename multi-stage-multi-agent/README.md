# Multi Stage Multi Agent

Set up a **multi-stage Jenkins pipeline** where each stage runs on a **different agent**. This approach is useful when working with **multi-language applications** or applications that have **conflicting dependencies**, because each stage can run in its own environment with the required tools and configurations.
